using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class refilnotice : MonoBehaviour
{

    public int openr;
    // Start is called before the first frame update
    void Start()
    {
        openr = 0;
    }

    // Update is called once per frame
    void Update()
    {

    }
}
