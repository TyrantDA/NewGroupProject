using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class enimyalive : MonoBehaviour
{

    public int alive;
    // Start is called before the first frame update
    void Start()
    {
        alive = 0;
    }

    // Update is called once per frame
    void Update()
    {

    }
}
